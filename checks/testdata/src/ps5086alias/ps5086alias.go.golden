package ps5086alias

import (
	ba "bytealias"
	b "bytes"
)

func aliasedPackages(data ba.Bytes) []byte {
	return b.ToUpper(data) // want `bytes.ToUpper already creates independent output but receives 1 throwaway Clone layer`
}
