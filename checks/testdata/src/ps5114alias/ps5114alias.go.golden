package ps5114alias

import fp "path/filepath"

func normalize(name string) string {
	return fp.Base(name) // want `filepath.FromSlash rescans the already-native result of filepath.Base through 2 redundant FromSlash layer`
}
