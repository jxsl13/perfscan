package ps5114alias

import fp "path/filepath"

func normalize(name string) string {
	return fp.Join("root", name) // want `filepath.FromSlash rescans the already-native result of filepath.Join through 2 redundant FromSlash layer`
}
