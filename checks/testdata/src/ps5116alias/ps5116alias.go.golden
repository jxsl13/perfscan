package ps5116alias

func validate(s string, b []byte) (bool, bool) {
	return true, // want `utf8.ValidString validates strings.ToValidUTF8 output whose replacement already guarantees valid UTF-8; the result is always true`
		true // want `utf8.Valid validates bytes.ToValidUTF8 output whose replacement already guarantees valid UTF-8; the result is always true`
}
